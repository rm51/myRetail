I would use only post for both for more security rather than get

I also wouldn't hard code the db values but instead pull them from the config in say Kubernetes

pip install -r requirements.txt to install all necessary packages